/// Routes shared between User and Specialist flows.
/// Both [UserAppRoutes] and [SpecialistAppRoutes] must keep these paths identical.
class SharedRoutes {
  static const String splash = '/';
  static const String onboarding = '/onboarding';
  static const String roleSelection = '/role-selection';
}
